import { SelectCardDirective } from './select-card.directive';

describe('SelectCardDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectCardDirective();
    expect(directive).toBeTruthy();
  });
});
